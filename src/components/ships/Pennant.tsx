import pennantUrl from '@/assets/scene/pennant.png';

import styles from './Pennant.module.less';

/**
 * Вымпел старшего на рейде — снимок настоящего вымпела: красная звезда у шкаторины,
 * серп и молот ближе к косице, синяя полоса вдоль нижней кромки, косица раздвоенным хвостом.
 * Рисованный вымпел стоял здесь раньше и настоящим не выглядел: ровные заливки читались
 * значком, а не тканью, — а отметка эта одна такая на весь список, и подделка в ней заметна.
 *
 * Размер задаётся высотой строки: высота в `em` и ширина по пропорциям снимка (см. стили).
 */
export default function Pennant() {
    return <img className={styles.pennant} src={pennantUrl} alt="" aria-hidden="true" />;
}
