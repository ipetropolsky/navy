node_modules
build
# Сборка с бэкендом Firebase для test:e2e:firebase — отдельная папка нарочно,
# см. playwright.firebase.config.ts.
build-firebase
.idea
.local

# Собранные функции: firebase deploy пересобирает сам, в репозиторий не едут.
functions/lib

# Настройки подключения к Firebase: у каждого свои, в репозиторий не едут.
.env.local
.env.*.local

.claude/cache
.claude/settings.local.json
.CLAUDE.local.md
test-results
playwright-report
__pycache__/

# Следы эмуляторов: пишутся в корень при каждом запуске, у каждого свои.
firebase-debug.log
firestore-debug.log
ui-debug.log
