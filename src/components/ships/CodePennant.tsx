import codePennantUrl from '@/assets/scene/code-pennant.png';

import styles from './CodePennant.module.less';

/**
 * Ответный вымпел — снимок настоящего: пять полос, красная и белая через одну, косица клином.
 * По своду сигналов его поднимают, когда сигнал разобран, и он же стоит вместо повторяемого
 * знака — то есть говорит не о корабле, а о самой передаче. Этим он здесь и помечает
 * служебное: строчку, которую написал не человек, а канал.
 *
 * Вымпелов в приложении два, и путать их нельзя: этот про сообщение, а вымпел старшего
 * на рейде (`Pennant`) — про корабль и его положение в строю. Отсюда и два компонента
 * вместо одного с настройкой: общего у них только размер.
 *
 * Размер задаётся высотой строки: высота в `em` и ширина по пропорциям снимка (см. стили).
 */
export default function CodePennant() {
    return <img className={styles.codePennant} src={codePennantUrl} alt="" aria-hidden="true" />;
}
