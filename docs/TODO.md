# TODO

Отложенные задачи: то, что решено сделать, но не сейчас. Здесь только про долг и планы —
что уже сделано, лежит в [PROJECT.md](./PROJECT.md), в разделе «План по шагам».

Свежее — сверху. Старое подтягивается наверх, когда до него доходит очередь.

## №190. Переключатель «Ход / Якорь» вместо кнопки, и сигнал со значком

**Переключатель — новый общий контрол**, его в `ui/` нет. Вид такой: лежачая плашка со
скруглением в полный рост, внутри в ряд подписи положений, у выбранного — белая пилюля
поверх серого поля. Переключение с анимацией: пилюля переезжает к выбранному, а не гаснет
и зажигается на новом месте. Положений в общем случае может быть и три, а не два.

Заводится он в `src/components/ui` и по устройству созвучен кнопке и полю: своя геометрия,
свои кегли и поля из тех же переменных, никаких решений по месту (см.
[STYLEGUIDE.md](./STYLEGUIDE.md)).

**Ширина у него своя.** Такой контрол нельзя тянуть шире содержимого: пилюля переезжает
по подписям, и растянутое поле развело бы их пустотой. Значит, в отличие от кнопок в `Actions`,
он ширину ряда не делит — стоит по своему содержимому. Ряд от этого перестаёт быть ровным,
и проще всего выровнять контролы карточки влево, а не растягивать их до кромок.

**В карточке корабля** он встаёт вместо нынешней кнопки с подменой подписи (`.swap`
в `ShipCard`): два положения, подписи те же — «Ход» и «Якорь». Подписи тут именно положения,
а не действия, — это и есть разница с кнопкой, которая предлагала обратное нынешнему.

**Кнопка сигнала** получает значок маячка и подпись «Подать сигнал», а на телефоне —
просто «Сигнал»: рядом с переключателем полная подпись в строку не встанет. Значок — в
`ui/icons.tsx`, короткая подпись — тем же способом, что «Уйти с рейда» и «Уйти» в списке
кораблей (см. `.wide` в `MembersList.module.less`): по ширине хозяина, а не по ширине экрана.

## №191. Область хвата у ручки разговора: 30px вместо 44px

Ручку и там, и там строит один общий кусок стилей (`.grip-handle` в `src/grip.less`),
и прибавка к области хвата у него — свой довесок к полю, который отрицательным margin
возвращается потоку: рисочка остаётся на месте, лишнее живёт только для указателя. Шторка
эту прибавку берёт вниз, на содержимое (`@grip-reach`, 22px), и выходит на обычные для
телефона 44px.

Разговору под кадром взять её негде. Вниз мешает лента: у неё свои нажатия на пузырях
и аватарках, и в самом верху ленты всегда стоит срез последней прокрученной реплики —
отбирать у неё пиксели значит ломать ответ по нажатию. Вверх мешает сама коробка: она
обрезана по своей кромке (`overflow: clip`), и вышедшее за неё не достаётся и указателю.
Прибавку даёт коридор (`CHAT_GRIP`, 16px, лежит на самом стыке): вместе с ним под палец
выходит 30px.

Чтобы дойти до 44px, придётся у кого-то отнять: либо расширить коридор вверх, на воду
(первые 20px кадра и так уходят под разговор — `CHAT_OVERLAP`), либо дать ручке довесок
вниз, на первую строку ленты. Оба решения не бесплатны, и выбирать тут не по стилям.

## Потерянная запись в эмуляторе «сервера» (не задача: решено не чинить)

Известный изъян эмулятора, который решено не чинить: состояние переезжает на Firebase,
и там этого класса ошибок нет вовсе — запись идёт транзакцией на сервере, а не в общей
на весь браузер строке. Пока живём с ним, поэтому он записан здесь: чтобы, встретив в кадре
два корабля на одной точке, не искать причину заново.

Замок из Web Locks выстроил вкладки в очередь, но потерянную запись до конца не убрал:
`localStorage` доходит до соседней вкладки не мгновенно. Записанное видно самому писавшему
сразу, а остальным — когда браузер разнесёт это по вкладкам, и следующий в очереди успевает
прочитать вчерашнее. Замеряно прямо в Chromium: две вкладки, «прочитал — прибавил — записал»
под общим замком, 160 проходов — от 8 до 17 чтений оказывались повторными. Пауза после записи
(1–4 мс) число снижает, но не до нуля: это гонка, а не задержка, и таймером она не лечится.

Как это видно глазами. Две вкладки переставляют корабли в один и тот же миг. Обе выбирают
место по своему снимку, обе видят точку свободной, обе на неё встают, и записанная второй
затирает чужую перестановку. В хранилище остаётся связный расклад, а вот вкладка — нет:
своё событие она применила, чужое тоже, и в кадре у неё два корабля стоят на одной точке
навсегда, до перезагрузки. Проверка «два корабля не встают на одно место, даже если выбрали
его разом» (`tests/channel.spec.ts`) ловит это примерно в трети прогонов — и ловит правильно,
так что до переезда она будет иногда краснеть.

## №184. Первое впечатление в пустом канале: полминуты пустой воды

Корабль, который встаёт в строй первым, идёт к дальней линии 26 секунд. Скорость тут ни при чём
и трогать её незачем: `MIN_SAIL_PACE` один на всех, тяжёлому кораблю и положено идти тяжело,
а разница во времени — это разница в длине пути, а не в проворности. Занят человек в это время
тоже не впустую: разговор, список кораблей, раскладка, шторки — всё работает на ходу, заперты
только своя форма и своя карточка, и заперты по делу.

Вопрос только в первом впечатлении: в пустом канале, кроме этого корабля, в кадре нет ничего,
и первые полминуты знакомства с приложением человек смотрит на пустую воду. Тут стоит подумать
не про скорость, а про то, чем занять кадр и куда ставить первого: заводить его ближе к берегу,
показывать рейд обжитым с самого начала — или ничего не менять, если пустой канал редок.

## №192. Миграции хранилища вместо сброса

Несовпадение `STORAGE_VERSION` стирает состояние и записывает демо-канал заново.
Это осознанная поблажка фазы разработки. До того, как в канале заведётся первый настоящий
разговор, на этом месте должна появиться миграция: по функции на переход версии, каждая
достраивает недостающее и ничего не удаляет. Правило — в
[BACKEND-API.md](./BACKEND-API.md#к-чужим-данным--бережно).

## №193. Приём сообщения печатается по буквам, лампа мигает по нему

**Зачем.** Сейчас «„Вымпел“ передаёт…» и мигание лампы живут на событии `typing`, которое
шлётся на каждое нажатие клавиши. Это работает, пока «сервер» — `BroadcastChannel` в соседней
вкладке, и перестанет работать на Firebase: там не открытый сокет, а `query` + `onSnapshot`,
то есть запись документа на каждую букву. Такое и стоит дорого, и упирается в предел записей
в один документ.

**Как вместо этого.** Ничего не передавать во время набора. Пришедшее сообщение получатель
печатает у себя по буквам — быстро и чуть неровно, как печатает человек, — и всё это время
у корабля-отправителя мигает лампа и в шапке стоит «передаёт». То есть индикация не живая,
а восстановленная из уже принятого текста, и на сервер не ложится ничего.

**Мигание длиннее печати**, поэтому Морзе идёт не за буквами, а за кусками: текст режется
примерно на 10 частей, каждая не короче 5 символов, и когда анимация печати доходит до границы
части, передача лампой переставляется в это же место текста. Никто не сверяет, что мигание
передаёт текст целиком, — это декорация. Но первые 5 символов и последние 5 передаются честно:
именно их и пытаются прочитать, если пытаются вообще.

**Свой набор — отдельный случай.** Когда текст набирает сам пользователь, его лампа мигает
по всему набранному, без разбиения: тут никакой передачи нет, всё локально. Учесть вставку
куска текста и вставку сразу целого сообщения — посимвольного потока в этом случае не будет.

**Что это меняет в контракте.** Метод `setTyping` и событие `typing` уходят целиком: они
не нужны ни для чего другого. До тех пор контракт помечен в
[BACKEND-API.md](./BACKEND-API.md) как единственное место, которое на Firebase не переносится
один в один.

## №194. Кадр шире окна на узком экране

**Зачем.** Кадр на телефоне почти квадратный, и рейд в нём ужат по бокам сильнее, чем нужно
глазу: перспектива читается плохо, корабли стоят тесно. Если дать кадру немного выйти за
кромки окна, рейд получит ту же ширину, что и на просторном экране, а перспектива —
свой разбег.

**Что при этом можно, а что нельзя.** Корма, ушедшая из вьюпорта, — не беда, а скорее
натуральнее: корабль виден не целиком, будто кадр его не вмещает. А вот нос обрезать нельзя
совсем: это главная часть силуэта, и на нём же стоит бортовой номер. Значит, вместе с расширением
кадра надо поправить расстановку: корабль не встаёт носом слишком близко к кромке, и поле
у носа считается по настоящему краю окна, а не по краю раздавшегося кадра. Поле у кормы при
этом можно и обнулить.

**Что проверить.** Что нос с номером виден целиком на всех ширинах, что расширение не рождает
горизонтальной прокрутки страницы и что рейд по-прежнему лежит на воде.

## №195. Одно пространство раскладок без ступенек

**Зачем.** Раскладок две, и переход между ними должен быть единственным переключением на всю
ширину экрана: постепенно сужая окно десктопа, человек получает мобильный вид без скачков
и ступенек — панель разговора сбоку уходит, и снизу выезжает шторка разговора, подпирая сцену
к верху. При растяжении наоборот: шторка уезжает вниз, сцена распахивается на весь экран,
а сбоку выезжает панель и поджимает её ширину.

**Что это значит на деле.** Все мерки кадра идут по одной шкале ширины (`--wide`), и порогов
по ширине нет нигде, кроме самой смены раскладки. Смена раскладки при этом не должна выглядеть
скачком: сцена в обеих во всю ширину окна, и меняется только то, откуда её поджимает разговор.

**Что проверить.** Покадрово: сужать окно через точку смены раскладки и смотреть, что кадр
не прыгает ни высотой, ни шириной, а разговор переезжает одним движением.

## №145. Орион целиком на распахнутой мобиле

Раньше в стоячем окне с низким разговором созвездие Ориона было видно полностью, теперь нет.
Вернуть. Смежно с «одним и тем же сектором неба в обеих раскладках».

## №147. Своя личность помнится по каналам

**Зачем.** Вернувшись в канал, человек снова заполняет форму с нуля, будто пришёл впервые.
Помнить надо не форму, а корабль: каким кораблём ты последний раз был именно в этом канале.
Тогда возврат в канал — это возврат на своё место, а не новая постановка в строй.

**Как это устроено.** В sessionStorage лежит связка `channelId: memberId` — по одной на канал.
Открыли канал: нашли в связке свой memberId, спросили у бэкенда корабль по нему и встали
в строй тем же самым. Сами параметры корабля — позывной, номер, силуэт, цвет, место — в
хранилище вкладки не кладём вовсе: они живут на бэкенде, и вкладка помнит только, кто она
здесь такая.

**Основное memberId.** Личность у человека одна, а корабли у неё разные: в одном канале
«Альбатрос», в другом «Гроза». Поэтому основной идентификатор — свой у sessionStorage
и в его пределах не меняется, а связка отвечает на вопрос «каким кораблём эта личность
ходит в этом канале». Так это ляжет и на будущую авторизацию: на её месте окажется
тот же основной идентификатор, только выданный не вкладкой, а входом в систему, — и связка
каналов переедет к нему, не меняя формы.

**Первый заход.** Канала в связке нет — форма пустая. Либо, если решим, подставленная
последним кораблём этой личности: позывной с номером человек всё равно поправит, а силуэт
и цвет чаще берут те же. Выбрать одно из двух, когда дойдут руки.

## №196. Дизайн-система вместо решений по месту

**Зачем.** Компонентов в `ui/` набралось много, а системы за ними нет: каждый решает свою
задачу так, как было удобно в тот день, когда его завели. Оттого одно и то же — поле по бокам,
ряд кнопок внизу, крестик в углу, полоска у верхней кромки — то живёт в компоненте, то
дописывается по месту у того, кто его показывает. Пройтись по всем и свести к последовательной
системе.

**Что должно получиться.** Слой общих компонентов, у каждого — прописанная геометрия, слоты
и набор вариаций:

- кнопка с типовыми вариациями (основная, вторичная, опасная, со значком);
- шторка: геометрия, слот под название, необязательный крестик, полоска у верхней кромки,
  слот под кнопки, точки остановки;
- выезжающая снизу панель — то, в чём сейчас живут формы;
- боковая панель (дровер) с изменяемой шириной;
- снекбар;
- поле ввода, подпись к полю, полоса кнопок внизу, кнопка шапки.

**Отдельным слоем — компоненты приложения.** Карточка корабля, заголовок блока с позывным
и флагами, сообщение в ленте и прочее, что знает про корабли и рейд, а не про геометрию.
Общие компоненты про рейд ничего не знают и знать не должны.

**Что проверить.** Что ни один прикладной компонент не дописывает чужую геометрию по месту:
поля, отступы и раскладку слотов задаёт тот, кто показывает, а содержимое только говорит,
что показать.

## №187, №188. Прогон проверок за минуту

Замер на четырёх ядрах после того, как сроки поджаты и открытие канала перестало ждать тишины
в сети: 148 проверок, 283 секунды суммы, два воркера — около двух с половиной минут стены плюс
двадцать секунд на сборку. Чтобы уложиться в минуту, нужно либо впятеро больше воркеров, либо
вдвое меньше суммы при четырёх. Рычагов четыре, и они очень разные по цене.

**Воркеры — единственный, который даёт минуту сам.** Их два не по нашему выбору, а по числу
ядер: Playwright берёт половину. На восьмиядерной машине выходит шесть воркеров и сорок семь
секунд; на четырёх ядрах минуты не будет никогда, сколько ни переписывай проверки. Стоит это
строчки в конфиге (`workers: '75%'`) и разбивки на шарды в CI — то есть железа, а не труда.

**Порог входа — половина суммы.** Каждая проверка платит около секунды за то, чтобы приложение
просто открылось: свой контекст, переход, загрузка картинок сцены, проявление. Сто сорок восемь
раз по секунде — это сто тридцать секунд из двухсот восьмидесяти. Снимается там ещё немало,
и дёшево: глухая пауза на проявление в `sceneReady` (сто шестьдесят миллисекунд на проверку)
и `WRITE_MS` в `join` (триста) заменяются ожиданием признака, а не времени. Вместе — секунд
сорок суммы за день работы.

**Перенос логики в юниты — секунд шестьдесят пять.** Разбор всего набора по тому, нужен ли
браузер, дал пять групп.

- _Геометрия_ (около шестидесяти проверок) — пропорция кадра, полоска дымки в семьдесят два
  пикселя, ширина кнопок, рост шторки, место Ориона, стык кадра и разговора. Браузер тут не
  средство, а предмет: измеряется посчитанная раскладка, и считать её умеет только он. Не
  переносится и не должно.
- _Жесты и ввод_ (около двадцати пяти) — свайпы, потяг за ручку, рывок, выделение текста
  протяжкой, фокус. Проверяется дорога от указателя до состояния; модель её не заменит.
- _Время и кадры_ (около двенадцати) — качка, замыкание воды, приезд без щели, лампа морзе.
  Смотрят на промежуточные кадры.
- _Логика, которая просто живёт в браузере_ (около тридцати) — вот отсюда и переносить.
  Расстановка: «на тесной линии уступает тот, кто мельче» (пятнадцать секунд), «соседняя линия
  остаётся на воде», «разброс по коридору одинаков», «на одной линии помещаются двое» — это
  алгебра `placement.ts`, и юниты у неё уже есть. Ход: «переходит по воде, не уходя из кадра»
  (одиннадцать секунд) — это `shipMotion.ts`. Бэкенд: две вкладки, старшинство, переоснащение —
  это `localBackend` и `notice`, а две вкладки эмулируются двумя бэкендами в одном процессе.
  Проверки ввода: `errors.spec` целиком. Раскладка как состояние: «форма окна выбирает
  раскладку», «ширина помнится отдельно от высоты» — это `useLayout.ts`, окно там подменяется
  числом. В браузере эта группа стоит около минуты и ждёт ходов корабля; в юнитах — доли
  миллисекунды. В e2e от каждой темы довольно одной проверки: правило доходит до экрана.
- _Двойники_ (несколько) — «места на рейде лежат на воде» стоит в наборе дважды, и такая пара
  не одна. Часть намеренно (разные окна), часть — нет.

**Ускорять анимации дальше не стоит.** Десятикратное ускорение уже упирается не в приложение,
а в браузер: переезд раскладки в этом темпе идёт три кадра экрана, и проверки, которые смотрят
на движение посередине, и так возвращают себе обычное время (`unhasten`). Двадцатикратное
сняло бы секунд двадцать с трёх самых долгих проверок и добавило бы флаков во все остальные.

**Порядок работ.** Воркеры и шарды; потом порог входа; потом перенос логики в юниты — он ещё
и надёжнее: гонка эмулятора, которая сейчас красит треть прогонов, в юнитах воспроизводится
нарочно, а не случайно. Модель интерфейса (ниже) делается ради ясности, а не ради минуты:
самая большая группа — геометрия — не переносится ни при какой модели.

## №189. Модель интерфейса и сцены: ход записывается, вид доигрывает

Мысль такая: состояние приложения — доска, действие — ход в нотации, а вид только доигрывает
изменение. Половина этого уже есть, и ложится оно хорошо.

**Что уже сделано.** `ChannelBackend` — это уже контракт «что изменилось»: события, а не снимки
(см. BACKEND-API.md). `member.place` — уже клетка: слот, коридор, смещение, разворот, сторона
входа. Перестановка корабля и есть «e2-e4»: меняется место, а путь, длительность и разворот
вид считает сам (`shipMotion`), и рисует переходом. Правила ходов — куда можно встать, кто кому
уступает, какие места закрыты островом — живут в `placement.ts` отдельно от показа. То есть
доска, фигуры, правила и нотация хода уже есть, и юниты у них тоже.

**Чего нет.** Модели самого интерфейса. Что открыто (список, карточка, форма, шторка прощания),
что выбрано, что подсвечено, где стоит разговор, что в фокусе — это размазано по состоянию
`App`, `SeaScene` и хуков. Поэтому всякая проверка про «открыл список — нажал строчку —
приехала карточка — шапка ушла под затемнение» вынуждена идти через DOM: единого места, где
это состояние можно спросить, нет.

**Что это даёт.** Одно состояние и набор намерений («открыть список», «выбрать место 9-left»,
«отпустить ручку со скоростью v») — и тогда юниты гоняют последовательности намерений и сверяют
модель, а браузеру остаётся доказывать одно: модель дошла до экрана. Новые проверки на переходы
между состояниями пишутся строчкой и идут миллисекунды.

**Чем это опасно.** Модель становится вторым источником правды: стоит виду решить что-нибудь
от себя — и проверки зеленеют впустую. Держится это одним правилом — вид рисуется из модели
и сам ничего не решает, — и небольшим набором проверок на стык. И это переборка состояния
`App` и `SeaScene`, а не правка проверок: работа на недели, посреди которых набор красный.
